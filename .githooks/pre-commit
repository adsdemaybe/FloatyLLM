#!/bin/sh
# Reject commits containing block/multiline comments in C/C++/CUDA files.
# Enable once with: git config core.hooksPath .githooks
python3 tools/lint_comments.py src tools || {
    echo "pre-commit: fix block comments above, or bypass with --no-verify"
    exit 1
}
